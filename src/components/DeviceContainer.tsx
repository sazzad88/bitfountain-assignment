import React, { useState } from "react";

function DeviceContainer() {
  return (
    <div className="main-container">
      <div className="section-title full-width">Available Devices</div>
    </div>
  );
}

export default DeviceContainer;
